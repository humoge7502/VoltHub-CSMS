# docs

Engineering record: `adr/` (monolith, TimescaleDB, outbox — decision/options/
verdict each), `perf.md` (honest laptop numbers + how to reproduce),
`demo-script.md` (12-minute beats with fallbacks), `resume-bullets.md`.
`openapi.json` is the full REST contract (49 paths / 53 operations) as a static
snapshot of the live `/api/v1/docs` spec — kept honest by the CI drift gate.
The full DA1→DA2→DA3 masterplan lives in `docs/masterplan/` + polished PDF `docs/VoltHub-CSMS-Engineering-Masterplan.pdf`.
