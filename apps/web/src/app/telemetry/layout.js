export const metadata = {
  title: 'Telemetry',
  description: 'Load curve and utilization heatmap from TimescaleDB continuous aggregates.',
};

export default function Layout({ children }) {
  return children;
}
