export const metadata = {
  title: 'Wallet',
  description: 'Prepaid wallet and itemized invoices — ledger append and balance decrement in one transaction.',
};

export default function Layout({ children }) {
  return children;
}
