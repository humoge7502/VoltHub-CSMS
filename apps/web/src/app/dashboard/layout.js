export const metadata = {
  title: 'Health grid',
  description: 'Station-scoped operator dashboard — revenue, energy, connector state and faults.',
};

export default function Layout({ children }) {
  return children;
}
