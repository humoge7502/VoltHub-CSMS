export const metadata = {
  title: 'Live session',
  description: 'Live OCPP charge session — metered kWh, power and estimated cost.',
};

export default function Layout({ children }) {
  return children;
}
