export const metadata = {
  title: 'Discover',
  description: 'Live charger availability across the Chennai corridor — filter by connector standard.',
};

export default function Layout({ children }) {
  return children;
}
