apiVersion: 1
datasources:
  - name: TimescaleDB
    type: postgres
    access: proxy
    url: timescale:5432
    database: volthub
    user: volthub
    secureJsonData: { password: volthub_dev_pwd }
    jsonData: { sslmode: disable, timescaledb: true }
