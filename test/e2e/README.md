# test:e2e — 7-step full journey (`test/e2e/run.js`)

Register → discover CCS2 → reserve → plug-in + ticks → stop/bill/pay/review →
operator fault triage → admin tariff version. Ephemeral port 4103.
`npm run test:e2e`. Demo insurance: if this is green, the live demo works.
